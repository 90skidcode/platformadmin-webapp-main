# secretlint substitutes for the plan's gitleaks (§17) -- gitleaks itself
# isn't a real npm-installable package (the "gitleaks" npm name is an
# unrelated name-squat, verified during Phase 0f setup); secretlint is a
# genuinely working, npm-native secret scanner with the same job.
#
# False positives go here as they're found (plan §19's ".gitleaksignore
# pass"), not disabled by turning off the rule.
pnpm-lock.yaml
package-lock.json
.next/**
coverage/**
